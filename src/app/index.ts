// App level

import localDBInstance from "../database/localDBInstance/dbInstance"

// Entry point for running the predictor model
const predict = () => {
    const dbInstance = new localDBInstance()
}